import React from "react";

import classes from "./Navigation.module.css";

const Navigation = props => {
  return <div className={classes.navigation}>Navigation</div>;
};

export default Navigation;
